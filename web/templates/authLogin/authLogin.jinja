<!DOCTYPE html>
<html>
    <head>
        <title>
            Листочек: авторизация
        </title>
        <link rel="apple-touch-icon" sizes="180x180" href="/resources/apple-touch-icon.png">
        <link rel="icon" type="image/svg+xml" href="/resources/favicon.svg">
        <link rel="icon" type="image/png" href="/resources/favicon.png">
        <link rel="manifest" href="/site.webmanifest">
        <link rel="mask-icon" href="/resources/safari-pinned-tab.svg" color="#5bbad5">
        <meta name="msapplication-TileColor" content="#fff3b4">
        <meta name="theme-color" content="#fff3b4">
        <link rel="stylesheet" type="text/css" href="/leafyy.css">
    </head>
    <body>
        <script src="/libraries/jquery.js"></script>
        <script src="/libraries/js-cookie.js"></script>
        <script src="/leafyy.js"></script>
        <script src="/authLogin.js"></script>
        <header class="block stretcher">
            <img src="/resources/banner.jpg" height="32">
        </header class="block stretcher">
        <span class="hidden" id="redirectAfter">
        {% if redirectAfter %}
        {{redirectAfter}}
        {% else %}
        /
        {% endif %}
        </span>
        <div class="block stretcher">
            <div class="bold grand">
                Авторизация
            </div>
        </div>
        <div class="block stretcher status-bar">
            <div id="status">
                {% if statusMessage %}
                {{statusMessage}}
                {% else %}
                Выполни вход, чтобы начать взаимодействовать с Листочком.
                {% endif %}
            </div>
            <div>
                <a class="action" href="#" onclick="hideStatusBar()">скрыть</a>
            </div>
        </div>
        <form class="block vertical" id="loginForm" action="/auth/finish?to={% if redirectAfter %}{{redirectAfter}}{% else %}/{% endif %}" method="post">
            <input type="text" name="username" id="username" class="forminp code bold grand" placeholder="имя пользователя...">
            <input type="password" name="password" id="password" class="forminp code bold grand" placeholder="пароль...">
            <input type="submit" id="confirm" class="action code bold grand" value="Вход">
        </form>
        <footer class="block stretcher fixed-bottom">
            <div>
            Листочек, система автоматического контроля растений.
            2023 год. Никакие права не защищены.
            </div>
            <div>
            {{version}}
            </div>
        </footer>
    </body>
</html>
