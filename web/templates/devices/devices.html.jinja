<!DOCTYPE html>
<html>

<head>
    <title>
        Иваров листочек: устройства
    </title>
    <link rel="stylesheet" type="text/css" href="devices.css">
</head>

<body>
    <header>
        <div id="title">
            Листочек
        </div>
        <nav>
            <a href="/">домой</a>
            <a href="/rules">правила</a>
            <a href="/devices">устройства</a>
        </nav>
    </header>
    <div class="textblock" id="actions">
        <input type="button" value="Добавить">
        <input type="button" value="Изменить">
        <input type="button" value="Удалить">
    </div>
    <div class="textblock" id="view">
        {% if devices %}
        <ul>
            {% for device in devices %}
            <li>
                <div class="rule">
                    <p>Имя: {{device.name}}</p>
                    <p>Статус: {{device.status}}</p>
                    <p>Описание: {{device.description}}</p>
                </div>
                {% endfor %}
            </li>
        </ul>
        {% else %}
        Нет устройств для отображения.
        {% endif %}
    </div>
</body>

</html>